#ifndef UTILS_H
#define UTILS_H

/// @brief Duplicates a char array(string).
/// @param s Is a char array(string) to be duplicated.
/// @return Newly allocated char array(string) as a pointer if successful, NULL if it fails.
char *my_strdup(const char *s);

#endif